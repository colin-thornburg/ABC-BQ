version: 2

models:
  - name: fct_orders
    group: sales_analytics
    access: public
    description: "The fact orders model provides a comprehensive table of order details."
    config:
      contract:
        enforced: false