version: 2

sources:
  - name: source_fresher_demo
    description: Source freshness
    database: colint_dev
    schema: dbt_cthornburg
    tables:
      - name: stg_orders_fresh_rows
        freshness:
          warn_after: {count: 6, period: hour}
        loaded_at_field: _ETL_LOADED_AT
